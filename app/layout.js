import "./globals.css";

export const metadata = {
  title: "Actualizar precios",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
